<UserControl x:Class="RSG.View.Views.DictionaryDetailsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:Views="clr-namespace:RSG.View.Views"
             xmlns:ViewModels="clr-namespace:RSG.View.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.DataContext>
        <ViewModels:DictionaryDetailsViewModel />
    </UserControl.DataContext>
    <Grid Background="White">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="5*"/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <ListView Margin="5" BorderBrush="#FF0A47FF" Background="#4C67E5F9" ContextMenu="{DynamicResource SharedContextMenu}"/>
            <Button Grid.Row="1" Command="{Binding RunCommand}"/>
        </Grid>
    </Grid>
</UserControl>
